# Required — /api/blockscout returns 500 without it.
# Get one at https://robinhoodchain.blockscout.com/account/api-key
BLOCKSCOUT_API_KEY=

# Optional. CoinGecko and DexScreener are used via their free public tiers.
